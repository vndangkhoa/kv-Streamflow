<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base TV Theme -->
    <style name="Theme.StreamFlixTV" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorPrimaryDark">@color/primary_dark</item>
        <item name="android:colorAccent">@color/accent</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        
        <!-- Leanback Specific -->
        <item name="defaultBrandColor">@color/primary</item>
        <item name="defaultBrandColorDark">@color/primary_dark</item>
        <item name="defaultSearchColor">@color/accent</item>
        <item name="defaultSearchBrightColor">@color/primary</item>
        <item name="overlayDimMaskColor">#33000000</item>
        <item name="overlayDimActiveLevel">20%</item>
    </style>

    <!-- Details Screen Theme -->
    <style name="Theme.StreamFlixTV.Details" parent="Theme.Leanback.Details">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:colorPrimary">@color/primary</item>
        <item name="defaultBrandColor">@color/primary</item>
    </style>

    <!-- Playback Theme -->
    <style name="Theme.StreamFlixTV.Playback" parent="Theme.Leanback">
        <item name="android:windowBackground">@color/player_background</item>
        <item name="playbackControlsAutoHideTimeout">3000</item>
    </style>

    <!-- Search Theme (Must be Leanback) -->
    <style name="Theme.StreamFlixTV.Search" parent="Theme.Leanback">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:colorPrimary">@color/primary</item>
        <item name="defaultBrandColor">@color/primary</item>
        <item name="defaultSearchColor">@color/accent</item>
    </style>

    <!-- Card Styles -->
    <style name="MovieCardStyle">
        <item name="lbImageCardViewType">Title</item>
        <item name="cardType">mainOnly</item>
    </style>

    <!-- Text Styles -->
    <style name="TitleText">
        <item name="android:textSize">32sp</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>

    <style name="DescriptionText">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/text_secondary</item>
        <item name="android:lineSpacingMultiplier">1.2</item>
    </style>

    <style name="MetadataText">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/text_secondary</item>
    </style>

</resources>
